name: stemcnv-check
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  ## General ##
  - python >=3.12
  - snakemake >=8.0
  - pathlib
  - apptainer
  ## may be needed on WSL for apptainer
  - squashfuse
  - fuse-overlayfs
  ## pip / python ##
  - pip
  - pip:
    - loguru
    - ruamel.yaml
    - pyfakefs
