name: stemcnv-check-vep
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  - bcftools
  # This needs to be compatible with the mehari-db version in the base __init__.py
  - mehari >=0.35.0,<0.36